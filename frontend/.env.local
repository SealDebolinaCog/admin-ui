# Prevent React from automatically opening browser
BROWSER=none

# Disable fast refresh to prevent flashing
FAST_REFRESH=false

# Set polling interval to reduce file watching conflicts
CHOKIDAR_USEPOLLING=true
CHOKIDAR_INTERVAL=1000
